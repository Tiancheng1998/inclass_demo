# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'inclass_demo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

pod 'SnapKit', '~> 4.0'

  # Pods for inclass_demo

  target 'inclass_demoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'inclass_demoUITests' do
    # Pods for testing
  end

end
